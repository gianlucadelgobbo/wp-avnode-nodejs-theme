doctype xml
urlset(xmlns='https://www.sitemaps.org/schemas/sitemap/0.9', xmlns:xhtml='https://www.w3.org/1999/xhtml')
  url
    loc=config.domain
    if (config.locales.length>1)
      each locale in config.locales
        if locale == config.default_lang
          xhtml:link(rel="alternate", hreflang=locale, href=config.domain)
        else
          xhtml:link(rel="alternate", hreflang=locale, href=config.domain + "/"+locale+"/")
    lastmod=isodate
    //changefreq daily
    priority 1.0
