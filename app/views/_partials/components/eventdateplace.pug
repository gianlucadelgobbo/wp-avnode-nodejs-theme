.dateplace
  if result.schedule[0].starttime || result.schedule[result.schedule.length-1].endtime || result.boxDate
      .dates
          include eventdate
  if result.boxVenue
      .places
          include place
