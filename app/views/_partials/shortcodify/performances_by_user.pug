ul.lists.row(itemscope itemtype="http://schema.org/ItemList")
  - var basepath = '/'+basepath+'/'
  each performance, i in body.performances
    - var css = "col-md-4"
    if (opts.params && performance.type.slug==opts.params) || !opts.params
      include ../boxes/box_performance

